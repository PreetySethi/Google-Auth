<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login | Google Auth</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap CSS (optional but helpful) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    .google-signin-link {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      text-decoration: none;
      color: #444;
      font-weight: 500;
      font-size: 16px;
      border: 1px solid #ddd;
      padding: 8px 14px;
      border-radius: 4px;
      background-color: white;
      transition: box-shadow 0.2s;
    }

    .google-signin-link:hover {
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
      text-decoration: none;
    }

    .google-logo {
      width: 20px;
      height: 20px;
    }
  </style>
</head>
<body class="bg-light d-flex justify-content-center align-items-center vh-100">

  <% if (user) { %>
    <div class="text-center">
      <h2>Welcome, <%= user.displayName %>!</h2>
      <img src="<%= user.photos[0].value %>" class="rounded-circle mt-2" width="80" />
      <p class="mt-3"><%= user.emails[0].value %></p>
      <a href="/dashboard" class="btn btn-success mt-3">Dashboard</a>
      <a href="/logout" class="btn btn-outline-danger mt-2">Logout</a>
    </div>
  <% } else { %>
    <div class="text-center">
      <h2 class="mb-3">Welcome to Google Auth Login</h2>
      <p class="text-muted mb-4">Click the Below Button </p>
      <a href="/auth/google" class="google-signin-link">
        <img src="https://developers.google.com/identity/images/g-logo.png" alt="Google logo" class="google-logo" />
        Sign in with Google
      </a>
    </div>
  <% } %>

</body>
</html>
